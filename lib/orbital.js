/**
 * The original file
 * @example
 * // returns nothing really
 * globalNS.method1(5, 10);
 * @example
 * // returns 3
 * globalNS.method(5, 15);
 * @returns {Number} Returns some great codes
 */
console.log("Hello World!");

(() => {
  fetch('/test/sizedup');
});
